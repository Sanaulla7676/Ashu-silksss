import { useEffect, useState } from 'react';
import { Package, Plus, Trash2, RefreshCw, Save } from 'lucide-react';
import { useAuth } from '../context/AuthContext';
import { getCatalogProducts, createCatalogProduct, updateCatalogProduct, deleteCatalogProduct } from '../services/catalog';
import '../admin.css';
const empty={name:'',category:'Kanjeevaram Silk',price:'',mrp:'',stock:'0',sku:'',colour:'',fabric:'',occasion:'',description:'',media:''};
export default function Admin(){const{user,loading}=useAuth();const[products,setProducts]=useState([]),[form,setForm]=useState(empty),[editing,setEditing]=useState(null),[busy,setBusy]=useState(false),[error,setError]=useState(''),[admin,setAdmin]=useState(false);useEffect(()=>{if(user)user.getIdTokenResult(true).then(r=>setAdmin(r.claims.admin===true)).catch(()=>setAdmin(false))},[user]);const load=async()=>{setBusy(true);try{setProducts(await getCatalogProducts())}catch(e){setError(e.message)}finally{setBusy(false)}};useEffect(()=>{if(admin)load()},[admin]);if(loading)return <section className="section"><div className="container empty"><h2>Loading admin...</h2></div></section>;if(!user)return <section className="section"><div className="container empty"><h2>Admin sign-in required</h2></div></section>;if(!admin)return <section className="section"><div className="container empty"><h2>Access denied</h2><p>Your Firebase account does not have the admin claim.</p></div></section>;const set=(k,v)=>setForm(x=>({...x,[k]:v}));const save=async e=>{e.preventDefault();setBusy(true);try{const data={...form,price:Number(form.price),mrp:Number(form.mrp||form.price),stock:Number(form.stock)};editing?await updateCatalogProduct(editing,data):await createCatalogProduct(data);setForm(empty);setEditing(null);await load()}catch(e){setError(e.message)}finally{setBusy(false)}};const edit=p=>{setEditing(p.id);setForm({...empty,...p,price:String(p.price??''),mrp:String(p.mrp??''),stock:String(p.stock??'0')})};const remove=async id=>{if(!confirm('Delete this product?'))return;setBusy(true);try{await deleteCatalogProduct(id);await load()}catch(e){setError(e.message)}finally{setBusy(false)}};return <section className="section page-section"><div className="container"><div className="section-head"><div><span className="eyebrow">Ashu Silks</span><h2>Admin Dashboard</h2><p>Manage the live Firestore product catalogue.</p></div><button className="btn ghost" onClick={load} disabled={busy}><RefreshCw size={17}/> Refresh</button></div>{error&&<div className="checkout-card" style={{marginBottom:18,color:'#b42318'}}>{error}</div>}<div className="admin-grid"><form className="checkout-card form" onSubmit={save}><h3><Package size={20}/> {editing?'Edit product':'Add product'}</h3>{['name','category','price','mrp','stock','sku','colour','fabric','occasion','media'].map(k=><input key={k} required={['name','category','price','stock','sku'].includes(k)} placeholder={k[0].toUpperCase()+k.slice(1)} value={form[k]} onChange={e=>set(k,e.target.value)}/>)}<textarea placeholder="Description" value={form.description} onChange={e=>set('description',e.target.value)}/><div className="checkout-actions"><button type="button" className="btn ghost" onClick={()=>{setEditing(null);setForm(empty)}}>Clear</button><button className="btn primary" disabled={busy}>{editing?<Save size={17}/>:<Plus size={17}/>} {editing?'Save changes':'Add product'}</button></div></form><div className="admin-list"><h3>Live products ({products.length})</h3>{products.map(p=><article className="admin-product" key={p.id}><div><b>{p.name}</b><span>{p.sku} · {p.category}</span><span>₹{p.price} · Stock: {p.stock}</span></div><div className="admin-actions"><button className="btn ghost" onClick={()=>edit(p)}>Edit</button><button className="icon" onClick={()=>remove(p.id)} aria-label="Delete"><Trash2 size={17}/></button></div></article>)}</div></div></div></section>}
