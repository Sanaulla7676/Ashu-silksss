import { useEffect,useState } from 'react';
import { RefreshCw } from 'lucide-react';
import { useAuth } from '../context/AuthContext';
import { getAllOrders, updateOrderStatusAdmin } from '../services/admin';
import { money } from '../utils';
import '../admin.css';
export default function AdminOrders(){const{user,loading}=useAuth();const[admin,setAdmin]=useState(false),[orders,setOrders]=useState([]),[busy,setBusy]=useState(false),[error,setError]=useState('');useEffect(()=>{if(user)user.getIdTokenResult(true).then(r=>setAdmin(r.claims.admin===true)).catch(()=>setAdmin(false))},[user]);const load=async()=>{setBusy(true);try{setOrders(await getAllOrders())}catch(e){setError(e.message)}finally{setBusy(false)}};useEffect(()=>{if(admin)load()},[admin]);if(loading)return <section className="section"><div className="container empty"><h2>Loading...</h2></div></section>;if(!user||!admin)return <section className="section"><div className="container empty"><h2>Access denied</h2></div></section>;return <section className="section page-section"><div className="container"><div className="section-head"><div><span className="eyebrow">Ashu Silks</span><h2>Orders</h2><p>Manage customer orders and fulfillment status.</p></div><button className="btn ghost" onClick={load} disabled={busy}><RefreshCw size={17}/> Refresh</button></div>{error&&<p className="muted">{error}</p>}<div className="admin-list">{orders.map(o=><article className="admin-product" key={o.id}><div><b>{o.id}</b><span>{o.address?.name} · {o.address?.phone}</span><span>{o.items?.length||0} item(s) · {money(o.total)} · {o.address?.city}</span></div><select value={o.status||'pending'} onChange={async e=>{await updateOrderStatusAdmin(o.id,e.target.value);setOrders(xs=>xs.map(x=>x.id===o.id?{...x,status:e.target.value}:x))}}><option>pending</option><option>confirmed</option><option>processing</option><option>shipped</option><option>delivered</option><option>cancelled</option></select></article>)}</div></div></section>}
